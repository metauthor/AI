# Output generated by @ua_chatgpt_bot

Ваше питання: напиши код калькулятора на мові програмування c#

Відповідь: using System;

namespace CalcApp
{
  class Program
  {
    static void Main(string[] args)
    {
      // Змінна для обрахунку результату
      double result = 0;
      // Змінна для продовження обрахунку
      bool exitFlag = true;
      // Змінна для очікування подальшої дії
      bool continueFlag = true;

      while(exitFlag == true)
      {
        // Виведення повідомлення індикативної дії 
        if (continueFlag == true)
        {
          Console.Write("Ваша дія: ");
        } else
        {
          Console.Write("Ви вирішили продовжити? [y/N] ");
        }

        // Перевірка даних
        string action = Console.ReadLine();

        // Виконання заданої дії
        if (continueFlag == true)
        {
          // Математичні операції
          if (action == "+" || action == "-" ||
            action == "*" || action == "/")
          {
            // Зчитування чисел
            double n1, n2;
            // Перше число
            Console.Write("Перше число: ");
            n1 = TakeNumberInput();
            // Друге число
            Console.Write("Друге число: ");
            n2 = TakeNumberInput();
            // Обрахунок
            switch(action)
            {
              case "+":
                result = n1 + n2;
                break;
              case "-":
                result = n1 - n2;
                break;
              case "*":
                result = n1 * n2;
                break;
              case "/":
                result = n1 / n2;
                break;
            }
            Console.WriteLine($"Результат: {result}");
            // Припинення/продовження циклу
            continueFlag = false;
          }
          // Завершення роботи
          else if (action.ToLower() == "exit")
          {
            Debug.WriteLine("Роботу завершено");
            exitFlag = false;
            continueFlag = false;
          } 
          else 
          {
            Console.WriteLine("Неприпустима дія");
            continueFlag = false;
          }
        }
        else if(action.ToLower() == "y")
        {
          continueFlag = true;
        } 
        else
        {
          Debug.WriteLine("Роботу завершено");
          exitFlag = false;
        }
      }
    }

    static double TakeNumberInput()
    {
      // Перевірка вхідних даних
      while (true)
      {
        if (Double.TryParse(Console.ReadLine(), out double number))
        {
          return number;
        }
        else
        {
          Console.WriteLine("Неприпустиме значення, будь ласка, введіть число:");
        }
      }
    }
  }
}

