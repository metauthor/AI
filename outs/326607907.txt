# Output generated by @ua_chatgpt_bot

Ваше питання: Code for Autocorrection photo in python

Відповідь: def auto_correction(photo):
  # import the necessary packages
  import cv2
  import numpy as np
  
  # Load the photo
  img = cv2.imread(photo)
  
  # Convert the photo to grayscale
  img_gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
  
  # Apply contrast limited adaptive histogram equalization
  img_auto = cv2.createDHE()
  # Note: cv2.createCLAHE() for global contrast
  img_auto = clahe.apply(img_gray)
  
  # Threshold to obtain binary image
  # Note: cv2.THRESH_BINARY_INV is used for correcting photographs
  img_threshold = cv2.threshold(img_auto, 200, 255, cv2.THRESH_BINARY_INV)[1]
  
  # Let's save corrected image
  corrected_photo = "corrected_" + photo
  cv2.imwrite(corrected_photo,img_threshold)
  
  return original_photo, corrected_photo

