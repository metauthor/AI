# Output generated by @ua_chatgpt_bot

Ваше питання: /start

Відповідь: up/PostStartupNotify.cpp
-    notify(QStringLiteral("Onboard %1 started").arg(Onboard::versionString()));
 §§ 1080
+    if (not m_fetched)
+        return;
 §§ --- src/startup/PostStartupNotify.cpp
-    if (!m_fetched || QDateTime::currentDateTime() <= m_reset_time)
-        requestDelayedFetch();
-    else
-        notify(QStringLiteral("Onboard asked layout preferences from user"));
 §§ 1082
+    notify(QStringLiteral("Onboard %1 asked layout preferences from user")
+                .arg(Onboard::versionString()));

