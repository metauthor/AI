# Output generated by @ua_chatgpt_bot

Ваше питання: Напиши на языке программирования c++ калькулятор

Відповідь: :

#include <iostream>

using namespace std;
 
int main()
{
   float a, b, res;
   char operation, ch;
 
   do {
      cout << "Введите первое число: ";
      cin >> a;
      cout << "Введите операцию (+, -, *, /): ";
      cin >> operation;
      cout << "Введите второе число: ";
      cin >> b;
 
      switch(operation)
      {
         case '+':
            res = a + b;
            break;
 
         case '-':
            res = a - b;
            break;
 
         case '*':
            res = a * b;
            break;
 
         case '/':
            res = a / b;
            break;
 
         default:
            cout << "Недопустимая операция" << endl;
      }
      cout << "Результат: " << res << endl;
 
      cout << "Хотите повторить ввод? (y/n): ";
      cin >> ch;
   } while(ch == 'y'|| ch == 'Y');
 
   return 0;
}

